spec:
  name: posthog-api-proxy
  services:
    - name: posthog-api-proxy
      git:
        branch: master
        repo_clone_url: https://github.com/aidevshivam/tracking-proxy.git
      dockerfile_path: ./Dockerfile
      http_port: 80
